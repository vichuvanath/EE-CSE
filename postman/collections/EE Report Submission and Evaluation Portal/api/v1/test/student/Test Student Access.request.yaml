$kind: http-request
description: Temporary endpoint accessible to users with 'student' or 'admin' role.
url: "{{baseUrl}}/api/v1/test/student"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
settings: {}
examples: ./.resources/Test Student Access.resources/examples
order: 1000
