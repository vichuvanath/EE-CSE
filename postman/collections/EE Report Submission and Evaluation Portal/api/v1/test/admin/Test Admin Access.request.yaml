$kind: http-request
description: Temporary endpoint accessible ONLY to users with 'admin' role.
url: "{{baseUrl}}/api/v1/test/admin"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
settings: {}
examples: ./.resources/Test Admin Access.resources/examples
order: 1000
