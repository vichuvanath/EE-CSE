$kind: http-request
description: Get the unread notification count for the authenticated student.
url: "{{baseUrl}}/api/v1/student/notifications/unread-count"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
settings: {}
examples: ./.resources/Get Unread Notification Count.resources/examples
order: 1000
